# Memnet

Memenet provides in memory implementations of different interfaces in
**net** package. It can be used in testing of raw server implmentations
without spinning up a listener on an real os port.

For api documentation please visit.
